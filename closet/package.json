{
  "name": "closet",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js and FastAPI workspace for closet",
  "scripts": {
    "start:node": "node server.js",
    "start:api": ".venv/bin/python -m uvicorn api.main:app --reload --port 8000",
    "dev": "concurrently \"npm run start:node\" \"npm run start:api\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
  
}
